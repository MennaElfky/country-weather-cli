# Country Weather CLI

A simple Node.js command line application that asks the user to enter a country name, then displays the latitude, longitude, and current temperature.

## Features

- Takes country name from terminal
- Gets latitude and longitude using Mapbox Geocoding API
- Gets current temperature using WeatherAPI
- Handles invalid country names
- Handles API and network errors

## Run

npm install
node app.js
